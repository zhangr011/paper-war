# Issue 12: Tick pipeline assembly (10 Hz, 13 systems, remove ProjectileSystem)

## Parent

`.scratch/v1-design-overhaul/PRD.md`

## What to build

Update the game loop tick rate from 5 Hz to 10 Hz (100ms per tick). Assemble all 13 systems in the correct priority order. Remove ProjectileSystem and all references to ProjectileComponent.

Pipeline order by priority:
1. TerrainSystem (existing, updated in issue 02)
2. CommanderSystem (existing)
3. RecruitmentSystem (new, issue 07)
4. MovementSystem (existing, updated in issue 02)
5. SpatialHash update (existing)
6. CombatSystem (rewritten, issue 03)
7. DeathSystem (rewritten, issue 04)
8. LevelingSystem (new, issue 05)
9. FogSystem (existing)
10. ObjectiveSystem (new, issue 09)
11. AISystem (updated, issue 10)
12. SnapshotSystem (existing, updated in issue 13)

Remove:
- ProjectileSystem from the pipeline
- ProjectileComponent from component pool registration
- All references to ProjectileComponent in session.go

The ServerTicksPerSecond constant changes from 5 to 10. Speed calculations that depend on tick rate must be recalculated.

## Acceptance criteria

- [ ] ServerTicksPerSecond = 10
- [ ] All 12 systems registered in correct priority order
- [ ] ProjectileSystem removed from pipeline
- [ ] ProjectileComponent removed from pool registration
- [ ] No references to ProjectileComponent remain in session.go
- [ ] Tick interval = 100ms (time.Second / 10)
- [ ] Combat unit speed recalculated for 10 Hz
- [ ] Game loop tests pass at 10 Hz
- [ ] All existing game loop tests updated for new tick rate
- [ ] `go test ./pkg/game/ -v` passes

## Blocked by

- Issue 11 (needs all systems assembled in match lifecycle)

Status: ready-for-agent

**Labels:** `enhancement`, `ready-for-agent`

---

> *This was generated by AI during triage.*

## Triage Notes

**Category:** enhancement
**State:** ready-for-agent

**Reasoning:** Fully specified vertical slice from PRD (v1 design overhaul). Resolved through 107-question grilling session. All acceptance criteria, blockers, and user stories documented. Ready for AFK agent implementation.

**Labels:** `enhancement`, `ready-for-agent`

